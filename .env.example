# Azure OpenAI Configuration
# REQUIRED: Used for intelligent query classification and job re-ranking
# The QueryClassificationService uses Azure OpenAI to:
#   1. Classify queries as skill-based, location-based, or skill+location
#   2. Extract skills and locations from natural language queries
#   3. Handle queries like "Hey, I am a Java Developer. Can you find any job openings?"
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_GPT_DEPLOYMENT=gpt-4

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/ncs_job_search

# Optional: Model Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8888
LOG_LEVEL=info
